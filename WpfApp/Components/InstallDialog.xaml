<Window x:Class="WpfApp.Components.InstallDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="DrunkDeer Control"
        Icon="/Resources/drunkdeer-control-logo.ico"
        Height="380" Width="500"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True" Topmost="True"
        ResizeMode="NoResize"
        RenderOptions.ClearTypeHint="Enabled"
        UseLayoutRounding="True">

    <Border CornerRadius="12" BorderBrush="{StaticResource BorderMedium}" BorderThickness="1"
            Background="{StaticResource GlassBg}" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBg}"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="14,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Resources/drunkdeer-control-logo.png" Width="20" Height="20"
                               Margin="0,0,10,0"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="DRUNKDEER CONTROL" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextW2}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="2" Click="CloseButton_Click"
                            Style="{StaticResource MaterialDesignToolButton}"
                            Width="28" Height="28" Padding="0" Foreground="{StaticResource TextW3}">
                        <materialDesign:PackIcon Kind="WindowClose" Width="14" Height="14"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Body -->
            <Grid Grid.Row="1" Margin="22,18,22,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Lead -->
                <StackPanel Grid.Row="0" Margin="0,0,0,14">
                    <TextBlock Name="HeaderTitle" Text=""
                               FontSize="17" FontWeight="SemiBold"
                               Foreground="{StaticResource TextW}"/>
                    <TextBlock Name="HeaderSubtitle" Text=""
                               FontSize="12" Foreground="{StaticResource TextW3}"
                               TextWrapping="Wrap" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Detail card -->
                <Border Grid.Row="1" Style="{StaticResource GlassCard}" Margin="0,0,0,14">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Name="Row1Label" Text=""
                                       FontSize="11" Foreground="{StaticResource TextW3}"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1" Name="Row1Value" Text=""
                                       FontSize="11" Foreground="{StaticResource TextW}"
                                       FontFamily="Consolas" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Name="Row2Label" Text=""
                                       FontSize="11" Foreground="{StaticResource TextW3}"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1" Name="Row2Value" Text=""
                                       FontSize="11" Foreground="{StaticResource TextW}"
                                       FontFamily="Consolas" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Actions -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="CountdownText" Text=""
                               FontSize="10.5" Foreground="{StaticResource TextW3}"
                               VerticalAlignment="Center" Opacity="0.7"/>
                    <Button Grid.Column="1" Name="SecondaryButton"
                            Click="SecondaryButton_Click"
                            Style="{StaticResource GlassSubtleBtn}" Margin="0,0,8,0"/>
                    <Button Grid.Column="2" Name="PrimaryButton"
                            Click="PrimaryButton_Click"
                            Style="{StaticResource GlassPillBtn}" IsDefault="True"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
