<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- ===== GLASS PALETTE ===== -->
    <SolidColorBrush x:Key="GlassBg" Color="#01000000"/>
    <SolidColorBrush x:Key="SidebarBg" Color="#40202030"/>
    <SolidColorBrush x:Key="HeaderBg" Color="#60181824"/>
    <SolidColorBrush x:Key="CardBg" Color="#30FFFFFF"/>
    <SolidColorBrush x:Key="CardBgHover" Color="#40FFFFFF"/>
    <SolidColorBrush x:Key="RowBg" Color="#18FFFFFF"/>
    <SolidColorBrush x:Key="BorderThin" Color="#20FFFFFF"/>
    <SolidColorBrush x:Key="BorderMedium" Color="#30FFFFFF"/>
    <SolidColorBrush x:Key="TextW" Color="#F0FFFFFF"/>
    <SolidColorBrush x:Key="TextW2" Color="#A0FFFFFF"/>
    <SolidColorBrush x:Key="TextW3" Color="#60FFFFFF"/>
    <SolidColorBrush x:Key="AccentSoft" Color="#80FF8A65"/>
    <SolidColorBrush x:Key="GreenDot" Color="#80AAFFAA"/>
    <SolidColorBrush x:Key="GrayDot" Color="#60888888"/>
    <SolidColorBrush x:Key="RedSoft" Color="#90FF6B6B"/>

    <!-- ===== GLASS CARD ===== -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderThin}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <!-- ===== SIDEBAR PROFILE ITEM ===== -->
    <Style x:Key="GlassListItem" TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="Margin" Value="6,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextW}"/>
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
        </Style.Resources>
    </Style>

    <!-- ===== SIDEBAR BUTTON ===== -->
    <Style x:Key="GlassSidebarBtn" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="Margin" Value="6,4"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Foreground" Value="{StaticResource TextW2}"/>
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
        </Style.Resources>
    </Style>

    <!-- ===== PILL BUTTON ===== -->
    <Style x:Key="GlassPillBtn" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatMidBgButton}">
        <Setter Property="Padding" Value="16,6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="14"/>
    </Style>

    <!-- ===== SUBTLE BUTTON ===== -->
    <Style x:Key="GlassSubtleBtn" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Foreground" Value="{StaticResource TextW2}"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="6"/>
    </Style>

    <!-- ===== TRIGGER CHIP ===== -->
    <Style x:Key="GlassChip" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource RowBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderThin}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="0,0,6,6"/>
    </Style>

    <!-- ===== DIVIDER ===== -->
    <Style x:Key="GlassDivider" TargetType="Border">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Background" Value="{StaticResource BorderThin}"/>
        <Setter Property="Margin" Value="0,14"/>
    </Style>

</ResourceDictionary>
